\chapter{Vývar z funkcionální analýzy}
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

\section{Lineární prostory}
%%%%%%%%%%%%%%%%%%%%%%%%%%%

Nech» $\mathbb{R}$ je tìleso reálných èísel.
Neprázdnou mno¾inu $V$ nazveme \emph{vektorovým prostorem} (prvky nazýváme vektory)
nad tìlesem $\mathbb{R}$, je-li definována operace sèítání 
$+:V\times V\rightarrow V$ a operace násobení skalárem 
$\cdot:\mathbb{R}\times V\rightarrow V$ tak, ¾e:
\begin{enumerate}
   \item $(\forall x,y\in V)(x+y=y+x)$ -- komutativita $+$,
   \item $(\forall x,y,z\in V)((x+y)+z=x+(y+z))$ -- asociativita $+$,
   \item $(\exists 0\in V)(\forall x\in V)(0+x=x)$ -- existence nulového prvku 
           (neutrální prvek vzhledem ke sèítání),
   \item $(\forall x\in V)(\exists (-x)\in V)(x+(-x)=0)$ -- existence opaèného prvku,
   \item $(\forall \alpha,\beta\in\mathbb{R})(\forall x\in V)
          (\alpha(\beta x)=(\alpha\beta x))$,
   \item $(\forall x\in V)(1\cdot x = x)$,
   \item $(\forall \alpha,\beta\in\mathbb{R})(\forall x\in V)
         ((\alpha+\beta)x=\alpha x+\beta x)$ -- distributivita I,
   \item $(\forall \alpha\in\mathbb{R})(\forall x,y\in V)
         (\alpha(x+y)=\alpha x+\beta y)$ -- distributivita II.
\end{enumerate}
Pro $x,y\in V$ definujeme operaci odeèítání vztahem $x-y=x+(-y)$,
kde $-y$ je prvek opaèný k $y$. Symbol $\cdot$ pøi násobení budeme zpravidla
vynechávat.

Dùsledky definice: nulový prvek je jediný, ke ka¾dému prvku z $V$ existuje
právì jeden opaèný prvek, pro ka¾dé $x\in V$ platí $-x=(-1)\cdot x$
a $0\cdot x = 0$ (pozor: nula na levé stranì je nula v $\mathbb{R}$, nula na pravé
stranì je nulový prvek ve $V$!).

Podmno¾ina $V_0\subset V$ vektorového prostoru $V$ je \emph{podprostor}
prostoru $V$, pokud je uzavøená vzhledem ke sèítání vektorù a násobení skaláru
a vektoru; znaèíme $V_0\subset\subset V$.

\begin{ex}
   Pøíkladem vektorového prostoru je prostor $n$-tic ($n\in\mathbb{N}$)
   reálných èísel $\mathbb{R}^n$ s obvyklou definicí sèítání a násobení skalárem.
   Nulovým prvkem je vektor $(0,\ldots,0)$.
\end{ex}

\begin{ex}
   Mno¾ina $C[a,b]$ spojitých funkcí na uzavøeném intervalu je vektorový prostor
   (souèet spojitých funkcí je spojitá funkce, násobek skaláru a spojité funkce je
   opìt spojitá funkce).
   Nulový prvek je nulová funkce na $[a,b]$.
\end{ex}

\begin{ex}
   Mno¾ina $P_n[a,b]$ polynomù stupnì nejvý¹e $n\in\mathbb{N}$, 
   tj. v¹ech funkcí ve tvaru
   \[
      p:\;t\mapsto\sum\limits_{i=0}^{n}\alpha_i t^i, \; t\in[a,b],
   \]
   kde $\{\alpha_i\}_{i=0}^n\subset\mathbb{R}$.
   Proto¾e ka¾dý polynom je spojitá funkce a $P_n[a,b]$ je uzavøený vzhledem
   ke sèítání a násobení skalárem, platí $P_n[a,b]\subset\subset C[a,b]$.
\end{ex}

\section{Banachovy a Hilbertovy prostory}
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

Zobrazení $\|\cdot\|:\;V\rightarrow \mathbb{R}$ definované na lineárním prostoru $V$
nazveme \emph{normou} na $V$, jestli¾e
\begin{enumerate}
   \item $(\forall x\in V)(\|x\|=0\;\Leftrightarrow x=0)$,
   \item $(\forall x,y\in V)(\|x+y\|\leq\|x\|+\|y\|)$ -- trojúhelníková nerovnost,
   \item $(\forall \alpha\in\mathbb{R})(\forall x\in V)(\|\alpha x\|=|\alpha|\|x\|)$
         -- homogenita.
\end{enumerate}

Dùsledky definice:
\begin{itemize}
   \item $(\forall x\in V\setminus\{0\})(\|x\|>0)$:
         je-li $x\in V$, pak z trojúhelníkové rovnosti a homogenity plyne
         $0=\|x-x\|\leq\|x\|+\|-x\|=\|x\|+\|x\|=2\|x\|$, tak¾e $\|x\|\geq 0$;
         ale rovnost nastane jen v pøípadì, ¾e $x=0$;
   \item $(\forall x,y\in V)(|\|x\|-\|y\||\leq \|x-y\|)$;
   \item norma je spojitá funkce prvkù z $V$.
\end{itemize}

Prostor $V$ s definovanou normou $\|\cdot\|$ nazýváme 
\emph{normovaný lineární prostor}.

\begin{ex}
   Nech» $x\in\mathbb{R}^n$, $x=(\alpha_1,\ldots,\alpha_n)$.
   Na $\mathbb{R}^n$ mù¾eme definovat normy:
   \[
         \|x\|_1 \equiv \sum\limits_{i=1}^n |\alpha_i|,\;\;
         \|x\|_2 \equiv \sqrt{\sum\limits_{i=1}^n \alpha_i^2},\;\;
         \|x\|_{\infty} \equiv \max\limits_{i=1,\ldots,n}|\alpha_i|.
   \]
\end{ex}

\begin{ex}
   Nech» $f\in C[a,b]$.
   Analogicky jako v pøípadì $\mathbb{R}^n$ lze na $C[a,b]$ definovat normy:
   \[
      \begin{split}
         \|f\|_1 \equiv \int\limits_a^b|f(t)|\,dt,\;\;
         \|f\|_2 \equiv \sqrt{\int\limits_a^b|f(t)|^2\,dt},\;\;
         \|f\|_{\infty} \equiv \max\limits_{t\in[a,b]}|f(t)|.
      \end{split}
   \]
\end{ex}

Úplné prostory: Normovaný prostor $V$ je \emph{úplný}, jestli¾e ka¾dá 
Cauchyovská posloupnost%
\footnote{Posloupnost $\{x_n\}\subset V$ je Cauchyovská, pokud 
$(\forall \varepsilon<0)(\exists N\in\mathbb{N})(\forall m,n>N)
(\|x_m-x_n\|<\varepsilon)$. Jinými slovy, pokud jsou si prvky této posloupnosti
libovolnì blízké poèínaje nìjakým indexem $N$. Platí, ¾e ka¾dá konvergentní
posloupnost je Cauchyovská, naopak to v¹ak platit nemusí.}
ve $V$ konverguje k prvku ve $V$.
Úplnému normovanému lineárnímu prostoru øíkáme \emph{Banachùv prostor}.

\begin{ex}
   Tìleso reálných èísel $\mathbb{R}$ chápáno jako vektorový prostor nad $\mathbb{R}$
   s normou $\|x\|=|x|$, $x\in\mathbb{R}$, je Banachùv prostor,
   nebo» ka¾dá Cauchyovská posloupnost reálných èísel je konvergentní v $\mathbb{R}$.
   Stejnì tak prostor $\mathbb{R}^n$, $n\in\mathbb{N}$ je Banachùv.
\end{ex}

\begin{ex}
   Tìleso $\mathbb{Q}$ racionálních èísel chápáno jako vektorový nad $\mathbb{Q}$
   není Banachùv prostor, nebo» $\{x_n\}\subset\mathbb{Q}$, kde
   $x_n\equiv (1+n^{-1})^n$, $n\in\mathbb{N}$, konverguje v $\mathbb{R}$
   k èíslu $e=\exp(1)\not\in\mathbb{Q}$.
\end{ex}

\begin{ex}
   Prostor $C[a,b]$ s normou $\|\cdot\|_{\infty}$
   je Banachùv prostor, nebo» konvergence spojitých funkcí v normì 
   $\|\cdot\|_{\infty}$ je ekvivalentní stejnomìrné konvergenci spojitých funkcí,
   její¾ limita je spojitá funkce na $[a,b]$.
\end{ex}

\begin{ex}
   Prostor $C[a,b]$ s normou $\|\cdot\|_2$ není Banachùv.
   Pro jednoduchost uva¾ujme $a=-1$ a $b=1$ a pro $n\in\mathbb{N}$ definujme
   \[
      f_n\,:t\mapsto\begin{cases}
          -1&\text{pro }t\in(-1,-\frac{1}{n})\\
           1&\text{pro }t\in(\frac{1}{n},1)\\
           nt&\text{pro }t\in[-\frac{1}{n},\frac{1}{n}].
           \end{cases}
   \]
   Zøejmì $\{f_n\}\subset C[-1,1]$.
   Definujeme-li dále
   \[
      f\,:t\mapsto\begin{cases}
          -1&\text{pro }t\in(-1,0)\\
           1&\text{pro }t\in(0,1)\\
           0&\text{pro }t=0.
           \end{cases}
   \]
   Samozøejmì $f\not\in C[-1,1]$.
   Posloupnost $\{f_n\}$ v¹ak konverguje k $f$ v normì $\|\cdot\|_2$, nebo»
   \[
     \begin{split}
      \|f-f_n\|_2&=\int\limits_{-1}^1|f(t)-f_n(t)|\,dt
                  =\int\limits_{-\frac{1}{n}}^0 (1+nt)\,dt
                  +\int\limits_0^{\frac{1}{n}}(1-nt)\,dt
                  =\frac{1}{2n}+\frac{1}{2n}=\frac{1}{n}.
     \end{split}
   \]
   Proto¾e $\frac{1}{n}\rightarrow 0$ pro $n\rightarrow\infty$,
   také $\|f-f_n\|_2\rightarrow 0$, tak¾e $f_n\rightarrow f$.
   Na¹li jsme tedy posloupnost spojitých funkcí, která nekonverguje ke spojité funkci
   v normì $\|\cdot\|_2$.
\end{ex}

Poznámka: Odpovìï na otázku, zda je normovaný prostor Banachùv, 
mù¾e silnì záviset na zvolené normì, jak bylo zøejmé z pøedchozího pøíkladu.
Na prostorech koneèné dimenze (prostory s koneènou bází) je odpovìï jasná:
je-li $\{x_1,\ldots,x_n\}\subset V$ báze $V$ a pro $x\in V$
je $x=\sum\limits_{i=1}^n\alpha_ix_i$ ($\{\alpha_i\}_{i=1}^n\subset\mathbb{R}$)
a definujeme-li $\|x\|_{\infty}\equiv\max\limits_{i=1,\ldots,n}|\alpha_i|$,
lze ukázat, ¾e $V$ je pak úplný v této normì, a tedy Banachùv.
Proto¾e jsou v prostorech koneèné dimenze v¹echny normy 
ekvivalentní%
\footnote{Dvì normy $\|\cdot\|_a$ a $\|\cdot\|_b$ na $V$ jsou ekvivalentní,
pokud existují kladné konstanty $\alpha$ a $\beta$ takové, ¾e
$(\forall x\in V)(\alpha\|x\|_a\leq\|x\|_b\leq\beta\|x\|_a)$.},
jsou koneènì-dimenzionální normované prostory úplné (Banachovy)
v ka¾dé normì!

Nech» $V$ je lineární vektorový prostor.
Zobrazení $\langle\cdot,\cdot\rangle:\,V\times V\rightarrow\mathbb{R}$
nazveme skalární souèin na $V$, pokud platí:
\begin{enumerate}
   \item $(\forall x\in V)(\langle x,x\rangle\geq 0\;
            \&\;\langle x,x\rangle=0\;\Leftrightarrow x=0)$,
   \item $(\forall x,y\in V)(\langle x,y\rangle=\langle y,x\rangle)$ -- symetrie,
   \item $(\forall \alpha\in V)(\forall x,y\in V)
          (\langle\alpha x,y\rangle=\alpha\langle x,y\rangle)$ -- homogenita,
   \item $(\forall x,y,z\in V)
          (\langle x,y+z\rangle = \langle x,y\rangle+\langle x,z\rangle)$ 
          -- aditivita.
\end{enumerate}
Prostor $V$ s definovaným skalárním souèinem nazýváme
\emph{unitární prostor}, popø. Euklidùv prostor.

Dùsledky definice:
\begin{itemize}
   \item $(\forall \alpha\in V)(\forall x,y\in V)
          (\langle x,\alpha y\rangle=\alpha\langle x,y\rangle)$;
   \item $(\forall x,y,z\in V)
          (\langle x+y,z\rangle = \langle x,z\rangle+\langle y,z\rangle)$;
   \item Schwarzova nerovnost: $(\forall x,y\in V)
         (|\langle x,y\rangle|\leq\sqrt{\langle x,x\rangle}\sqrt{\langle y,y\rangle})$.
\end{itemize}

Definujeme-li pro $x\in V$ $\|x\|=\sqrt{\langle x,x\rangle}$,
dostaneme porovnáním s definicí normy za pou¾ití vlastností skalárního souèinu
a dùsledkù této definice, ¾e $\|\cdot\|$ je norma na $V$.
Je-li tedy prostor $V$ unitární, je zároveò normovaný.
Je-li navíc $V$ úplný v této normì, tj. Banachùv, nazývá se \emph{Hilbertùv prostor}.

\begin{ex}
   Nech» $x,y\in\mathbb{R}^n$, $x=(\alpha_1,\ldots,\alpha_n)$,
   $x=(\beta_1,\ldots,\beta_n)$.
   Na $\mathbb{R}^n$ lze definovat Euklidùv skalární souèin vztahem
   \[
      \langle x,y\rangle=\sum\limits_{i=1}^n \alpha_i \beta_i.
   \]
   Tento skalární souèin definuje normu $\|\cdot\|_2$ na $\mathbb{R}^n$.
\end{ex}

\begin{ex}
   Nech» $f,g\in C[a,b]$.
   Obdobnì i zde mù¾eme definovat skalární souèin vztahem
   \[
      \langle x,y\rangle=\int\limits_a^b f(t)g(t)\,dt.
   \]
   Tento skalární souèin definuje normu $\|\cdot\|_2$ na $C[a,b]$.
\end{ex}

Øekneme, ¾e dva vektory $x,y\in V$ jsou \emph{ortogonální}, 
pokud $\langle x,y\rangle=0$.

